# 翻译风格指南

本指南用于统一 Antigravity 中文语言包的文案风格，适用于人工润色与 AI 候选翻译生成。

## 目标

- 优先保证 **自然、清晰、像真实产品界面**
- 保持 **AI / 开发工具领域术语一致**
- 避免机翻腔、书面腔和过度解释

## 核心术语

- Agent → 智能体
- Workspace → 工作区
- Conversation → 会话
- Artifact → 交付件
- Model → 模型
- Model quota → 模型配额
- AI Credits → AI 额度
- Telemetry → 使用数据
- Marketplace → 市场
- Knowledge → 知识库

如有冲突，以 `dicts/glossary.json` 为准。

## 文案规则

### 1. 按钮要短

- Save → 保存
- Cancel → 取消
- Dismiss → 忽略 / 关闭（按语境选）
- Retry / Try Again → 重试

### 2. 设置项像产品文案

- 避免逐词直译
- 优先使用中文产品界面里常见表达
- 能一句说清楚，就不要堆很多修饰词

### 3. 长说明优先重写，不要硬翻

例如：

- 不推荐：提供最强免干预运行能力，但风险也最高。
- 推荐：这样可以减少人工确认，但风险也最高。

### 4. 谨慎保留英文

以下情况通常保留英文：

- 品牌名
- 模型名
- 文件名
- 技术缩写（如 API、JSON、MCP）

### 5. 统一语气

- 默认使用自然、中性的中文界面语气
- 避免过度营销化表达
- 避免过度口语化表达
- 同一页面尽量统一使用“您”

## AI 候选翻译约束

如果使用 API 生成候选译文，必须同时满足：

1. 输出仅作为候选，不直接覆盖线上词典
2. 必须读取 `dicts/glossary.json`
3. 必须优先保证术语一致
4. 长文案允许意译，但不得改变原意
5. 生成结果必须人工审核后再写回词典
