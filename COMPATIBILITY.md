# 兼容性矩阵

本文档记录当前版本中文语言包的兼容性状态。

## 说明

- **已验证**：当前仓库已在真实环境中验证过
- **设计支持**：代码路径已覆盖，但尚未在当前仓库中完成实机验证
- **待验证**：欢迎社区补充反馈

## 平台与安装结构

| 平台 | 安装结构 | 状态 | 说明 |
| --- | --- | --- | --- |
| Windows 11 | 新版 `app.asar` | 已验证 | 当前维护环境已识别并适配 |
| Windows | 旧版 `resources/app` | 设计支持 | 保留旧版 HTML 注入与 checksum 更新逻辑 |
| macOS | 新版 `app.asar` | 设计支持 | 启动脚本、路径探测与 asar 注入逻辑已覆盖 |
| macOS | 旧版 `resources/app` | 设计支持 | 路径与备份/还原逻辑已覆盖 |

## Python / Node 环境

| 组件 | 状态 | 说明 |
| --- | --- | --- |
| `python` | 已支持 | Windows / macOS 均可直接作为主脚本入口 |
| `python3` | 已支持 | macOS 优先使用 |
| `py -3` | 已支持 | Windows 下作为 `python` 不可用时的回退方案 |
| `npx` | 已依赖 | 新版 `app.asar` 注入需要可用的 Node.js / npm / npx |

## 当前已知限制

1. 官方如果更改 `preload.js` 路径、asar 打包方式或前端结构，可能需要更新脚本后才能继续使用。
2. 第三方扩展页面、模型返回内容、非 Agent Manager 核心区域不保证全部覆盖。
3. 长文案已做模糊匹配，但如果原文改动较大，仍可能需要补词典或正则规则。

## 如何补充兼容性反馈

欢迎通过 Issue 提交以下信息：

- 操作系统版本
- Antigravity 版本
- 安装结构（旧版 `resources/app` / 新版 `app.asar`）
- 成功 / 失败结果
- 终端输出或截图
